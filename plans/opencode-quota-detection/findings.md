OpenCode native stderr with --print-logs exposes level=ERROR message="stream error", error.error="AI_APICallError: Usage limit reached for 5 hour. Your limit will reset at ...". Its internal retry can continue without stdout. Existing CM one-shot executor waits for communicate before observing stderr. Detect only this run's native error channel, not shared historical log files or assistant text. Generic HTTP 429 is insufficient evidence of exhausted allowance. Reset dates without timezone remain provider-local text.

Release validation exposed a pre-existing non-hermetic restart test that used host service-manager environment and invoked the local CM restart. Root confirmed the service running and explicitly mocked the marker-only test's service-manager predicate. Release worker test environment also excludes inherited service-manager markers; no production restart behavior changed.
