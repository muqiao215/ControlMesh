# ControlMesh v0.34.7

- Fix OpenCode auth so config-backed runtime state stays selectable even when runnable preflight is unavailable.
- Make `uv_tool` upgrades fall back to `python -m pip` when `uv` is missing.
