# Release v0.41.5

- Restore the default ControlMesh terminal as a chat-first experience: plain natural-language input now goes to the selected model.
- Keep ControlMesh runtime commands available in the same terminal, including `help`, `model`, `status`, `tasks list`, `agents`, `memory today`, `cron list`, and `native`.
- Update terminal startup/help copy to make model chat the primary path while preserving explicit `chat <message>` and `/chat <message>` forms.
