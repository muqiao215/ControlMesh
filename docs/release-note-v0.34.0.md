# ControlMesh v0.34.0

- Add Feishu group-chat policy controls with typed per-group overrides.
- Add group reply modes, thread-isolated routing, and group-level sender allowlists.
- Treat group replies to the bot as explicit triggers and allow owned slash commands to bypass mention gating.
- Persist Feishu replay/dedupe state across restarts with better group-aware session anchoring.
